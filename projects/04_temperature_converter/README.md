# 🌡️ Temperature Conversion Program

## 📌 Project Description

A simple command-line application that converts temperature between Celsius (°C) and Fahrenheit (°F).
The user selects the unit type and enters a temperature value, and the program performs the correct conversion.

---

## 🚀 What It Does

* Prompts the user to choose a temperature unit (C or F)
* Accepts a numeric temperature input
* Converts:

  * Celsius to Fahrenheit
  * Fahrenheit to Celsius
* Rounds the result to one decimal place
* Handles invalid unit input

---

## ▶️ How to Run It

1. Make sure Python 3 is installed.
2. Navigate to the project folder:

   ```bash
   cd projects/04_temperature_converter
   ```
3. Run the program:

   ```bash
   python main.py
   ```
4. Enter the unit and temperature value when prompted.

---

## 🧠 Concepts Used

* `input()` function
* Type casting (`float`)
* Conditional statements (`if`, `elif`, `else`)
* Arithmetic expressions
* Built-in `round()` function
* Basic input validation

---

## 🖥️ Example Output

```
Is this temperature in Celsius or Fahrenheit (C/F): C
Enter the temperature: 25
The temperature in Fahrenheit is: 77.0°F.
```
---

## 📸 Ouput

![Temperature convertor Output](https://github.com/ArhanBytes/python-mastery-Brocode/blob/main/projects/04_temperature_converter/output.png)

---